name: paging_plus_example
description: Example application for the paging_plus package
version: 2.0.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  paging_plus:
    path: ../

dev_dependencies:
  lints: ^5.1.1
